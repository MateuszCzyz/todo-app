class Task {
  constructor(id, title, description, importance, deadline, done) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.importance = importance;
    this.deadline = deadline;
    this.done = done;
  }
}

export default Task;
