services:
  postgres:
    image: postgres
    container_name: postgres-db
    ports:
      - 5430:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: task-db
    volumes:
      - /data/postgres